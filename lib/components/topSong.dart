import 'package:flutter/material.dart';

class TopSong extends StatelessWidget {
  const TopSong({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Column(
        children: [
          Text(
            "Top Song",
            style: TextStyle(color: Colors.white, fontSize: 20),
          ),
          Text(
            "Top Song",
            style: TextStyle(color: Colors.white, fontSize: 20),
          ),
          Text(
            "Top Song",
            style: TextStyle(color: Colors.white, fontSize: 20),
          ),
          Text(
            "Top Song",
            style: TextStyle(color: Colors.white, fontSize: 20),
          ),
        ],
      ),
    );
  }
}
